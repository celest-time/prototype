language: php
php:
- 5.6
- 7.0
- hhvm

cache:
  directories:
    - vendor
    - $HOME/.composer/cache
    - dl

before_install: composer self-update && cd php.time

install: composer install --prefer-dist; make tzdata
script: vendor/bin/phpunit