language: php
php:
- 5.6
- 7.0
- hhvm

cache:
  directories:
    - vendor
    - $HOME/.composer/cache

matrix:
  allow_failures:
    - php: hhvm

before_install: cd php.time

install: composer install --prefer-dist
script: vendor/bin/phpunit